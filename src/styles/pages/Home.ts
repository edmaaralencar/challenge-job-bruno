import styled from 'styled-components';

export const Container = styled.main`
    width: Min(90%, 1140px);
    margin: 0 auto;
`;

export const EnterprisesList = styled.div`
    display: flex;
    flex-direction: column;
    gap: 3.2rem;

    margin: 8.5rem 0 4rem 0;
`

